#pragma once

// Tags for tag dispatching in reading configuration file

namespace GeometryWars
{
	struct WindowTag {};
	struct FontTag {};
	struct PlayerTag {};
	struct EnemyTag {};
	struct BulletTag {};
}